export interface UploadCSVProps {
    workbookId: string;
    tableId: string;
}

export interface chatProps {
    workbookId: string;
    tableId: string;
}

export interface StandardChatProps {
    workbookId: string;
    tableId: string;
    chatId: string;
}